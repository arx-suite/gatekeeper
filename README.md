# Arx - Gatekeeper

Gatekeeper is a powerful, extensible, and developer-friendly data validation library for Rust — built on top of Garde   
It provides a seamless experience for validating data from any source — whether it’s HTTP requests, CLI inputs, config files, or internal structs — with rich error messages and simple declarative validation rules
